/* const myArray = [];

myArray[0] = "Jonathan";
myArray[1] = 2005;
myArray[2] = true;
 */
/* console.log(myArray);

//length property
console.log(myArray.length);

//last element in the array
console.log(myArray.length - 1);

console.log(myArray[1]);
 */

/* myArray.push("Home"); //adds a new element
const lastItem = myArray.pop(); //returns the last element
console.log(lastItem); */

const myArray = ["A", "B", "C", "D", "E", "F"];

const newArray = myArray.slice(2.5); //slice from position 2 to 5
console.log(newArray);
myArray.reverse(); //reverses the order of elements in the array
console.log(myArray);
//the .join( ) prints all the elements of the array as a string seperated with commas
//the .split() is used return the elements before and after the split argument in form of a new array
//const newArray = myArrayA.concat(myArrayB); is used to concatenate two different arrays into one larger array and assign to a new name
//the spread operator ...
